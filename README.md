# splitpay-docs
SplitPay — a plug-and-play SDK that any merchant can drop into their confirmation screen. After payment, the payer splits the amount, shareable links are generated, and friends pay back directly via UPI. No app required, no platform dependency.
